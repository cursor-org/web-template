const Header = () => {
  // const fps = useFps()
  // const isDev = process.env.NODE_ENV !== 'development'

  return (
    <header className="py-2 sticky top-0 z-[51] w-full border-border/40 backdrop-blur bg-background/95 supports-[backdrop-filter]:bg-background/60">
      Header
    </header>
  )
}

export default Header
